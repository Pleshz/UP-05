<Page x:Class="UP.Pages.ProjectsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UP.Elements"
      Height="450" Width="800" MinHeight="450" MinWidth="800" Title="ProjectsList">

    <Grid Margin="20">
        <Border Background="White" Padding="20" CornerRadius="10" VerticalAlignment="Stretch" Height="Auto">
            <DockPanel LastChildFill="True">

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
                    <TextBox x:Name="SearchBox" Width="200" Margin="0,0,10,0" 
                         Text=""
                         TextChanged="SearchBox_TextChanged"/>

                    <ComboBox x:Name="FilterBox" Width="160" Margin="0,0,10,0" SelectionChanged="FilterBox_SelectionChanged">
                        <ComboBoxItem Content="Все проекты" IsSelected="True"/>
                        <ComboBoxItem Content="Мои проекты"/>
                        <ComboBoxItem Content="Публичные проекты"/>
                    </ComboBox>

                    <ComboBox x:Name="SortBox" Width="180" SelectionChanged="SortBox_SelectionChanged">
                        <ComboBoxItem Content="По дате создания (новые)" IsSelected="True"/>
                        <ComboBoxItem Content="По дате обновления"/>
                        <ComboBoxItem Content="По имени (А-Я)"/>
                        <ComboBoxItem Content="По имени (Я-А)"/>
                    </ComboBox>

                    <Button Click="CreateProject" Content="Создать проект" Width="160" Height="35" 
                        Margin="10,0,0,0" Background="#28a745" Foreground="White" FontWeight="Bold" />
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ProjectsItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <local:ProjectItem/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</Page>
