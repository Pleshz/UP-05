<Page x:Class="UP.Pages.ProjectsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UP.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProjectsList">

    <Grid Background="#f5f7fa" Margin="20">
        <Border Background="White" Padding="20" CornerRadius="10" Height="Auto" VerticalAlignment="Stretch">
            <DockPanel LastChildFill="True">
                <Button Content="Создать проект" Width="160" Height="35" DockPanel.Dock="Top" Margin="0,0,0,10" Background="#28a745" Foreground="White" FontWeight="Bold" />
                <DataGrid x:Name="ProjectsDataGrid" AutoGenerateColumns="False" SelectionMode="Single" CanUserAddRows="False" FontSize="14" IsReadOnly="True" >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="2*"/>
                        <DataGridCheckBoxColumn Header="Публичность" Binding="{Binding IsPublic}" Width="100"/>
                        <DataGridTextColumn Header="Создан" Binding="{Binding CreatedAt}" Width="120" />
                        <DataGridTextColumn Header="Последнее обновление" Binding="{Binding UpdateAt}" Width="170" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</Page>
