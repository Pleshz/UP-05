<Page x:Class="UP.Pages.ProjectMembers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UP.Pages"
      Height="450" Width="800" MinHeight="450" MinWidth="800" Title="ProjectMembers" Background="#f5f7fa" >

    <Grid Margin="20">
        <Border Background="White" Padding="20" CornerRadius="10" MaxWidth="800" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Участники проекта"
                           FontSize="22" FontWeight="Bold"
                           Foreground="#222"
                           Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,15">
                    <TextBox x:Name="UserLoginBox"
                             Width="250" Height="35"
                             Margin="0,0,10,0"
                             FontSize="14"
                             VerticalContentAlignment="Center"
                             Text="Введите логин пользователя"/>
                    <Button Content="Добавить"
                            Width="120" Height="35"
                            Background="#28a745" Foreground="White"
                            FontWeight="Bold" Cursor="Hand"
                            Click="AddUserToProject"/>
                </StackPanel>

                <DataGrid x:Name="MembersGrid" 
                          Grid.Row="2"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="False"
                          Margin="0,0,0,15"
                          HeadersVisibility="Column"
                          RowHeaderWidth="0"
                          AlternatingRowBackground="#f9f9f9"
                          BorderBrush="#ddd"
                          BorderThickness="1"
                          FontSize="14"
                          SelectionMode="Single">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Логин" Binding="{Binding UserLogin}" Width="2*"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding UserEmail}" Width="2*"/>

                        <DataGridComboBoxColumn Header="Роль"
                                                SelectedItemBinding="{Binding Role, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Width="1.5*">
                            <DataGridComboBoxColumn.ItemsSource>
                                <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                    <sys:String>Creator</sys:String>
                                    <sys:String>Member</sys:String>
                                    <sys:String>Viewer</sys:String>
                                </x:Array>
                            </DataGridComboBoxColumn.ItemsSource>
                        </DataGridComboBoxColumn>

                        <DataGridTemplateColumn Header="Действия" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Удалить"
                                            Background="#dc3545"
                                            Foreground="White"
                                            Padding="5,2"
                                            FontSize="13"
                                            Cursor="Hand"
                                            Click="RemoveUserFromProject"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
                    <Button Content="Сохранить"
                            Width="140" Height="40"
                            Background="#007ACC" Foreground="White"
                            FontWeight="Bold" Cursor="Hand"
                            Click="SaveRoles"/>
                    <Button Content="Назад"
                            Width="140" Height="40"
                            Background="#555" Foreground="White"
                            FontWeight="Bold" Cursor="Hand"
                            Margin="10,0,0,0"
                            Click="BackToProject"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
