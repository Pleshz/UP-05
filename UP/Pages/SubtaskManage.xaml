<Page x:Class="UP.Pages.SubtaskManage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UP.Pages"
      mc:Ignorable="d" 
      Height="450" Width="800" MinHeight="450" MinWidth="800" Title="SubtaskManage">

    <Grid Margin="20">
        <Border Background="White" CornerRadius="10" Padding="20" BorderBrush="#ddd" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="HeaderText"
                           Text="Управление подзадачей"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,0,0,15"
                           HorizontalAlignment="Center"
                           Grid.Row="0"/>

                <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="0,0,0,10">
                    <TextBlock Text="Название:"/>
                    <TextBox x:Name="NameTextBox" Margin="0,0,0,10"/>

                    <TextBlock Text="Описание:"/>
                    <TextBox x:Name="DescriptionTextBox"
                             Height="60"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,10"/>

                    <TextBlock Text="Ответственный:"/>
                    <ComboBox x:Name="UserComboBox" Margin="0,0,0,10"/>

                    <TextBlock Text="Дедлайн:"/>
                    <DatePicker x:Name="DueDatePicker" Margin="0,0,0,10"/>
                </StackPanel>

                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,10,0,0">
                    <Button Content="Сохранить"
                            Width="120" Height="35"
                            Margin="0,0,10,0"
                            Background="#28a745"
                            Foreground="White"
                            FontWeight="Bold"
                            Click="Save_Click"/>
                    <Button x:Name="DeleteButton"
                            Content="Удалить"
                            Width="120" Height="35"
                            Margin="0,0,10,0"
                            Background="#dc3545"
                            Foreground="White"
                            FontWeight="Bold"
                            Visibility="Collapsed"
                            Click="Delete_Click"/>
                    <Button Content="Отмена"
                            Width="120" Height="35"
                            Background="#555"
                            Foreground="White"
                            FontWeight="Bold"
                            Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
